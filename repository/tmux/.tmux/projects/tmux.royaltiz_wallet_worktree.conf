source-file ~/.tmux/tmux.conf

setenv -g PROJECT ~/workspace/royaltiz/wallet/worktree
setenv -g PATH $PATH:~/workspace/royaltiz/wallet/worktree/node_modules/.bin
new-session -s 1_project -n cli -c ~/workspace/royaltiz/wallet/worktree
new-window -n vim -c ~/workspace/royaltiz/wallet/worktree
new-window -n llm -c ~/workspace/royaltiz/wallet/worktree
new-window -n job -c ~/workspace/royaltiz/wallet/worktree
new-window -n log -c ~/workspace/royaltiz/wallet/worktree
new-window -n app -c ~/workspace/royaltiz/wallet/worktree
select-window -t 1_project:cli
