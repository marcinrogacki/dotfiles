source-file ~/.tmux/tmux.conf

setenv -g PROJECT ~/workspace/royaltiz/platform/worktree
setenv -g ROY_CLI true
setenv -g PATH $PATH:~/workspace/royaltiz/platform/worktree/node_modules/.bin
new-session -s 1_project -n cli -c ~/workspace/royaltiz/platform/worktree
new-window -n vim -c ~/workspace/royaltiz/platform/worktree
new-window -n llm -c ~/workspace/royaltiz/platform/worktree
new-window -n job -c ~/workspace/royaltiz/platform/worktree
new-window -n log -c ~/workspace/royaltiz/platform/worktree
new-window -n app -c ~/workspace/royaltiz/platform/worktree
select-window -t 1_project:cli
