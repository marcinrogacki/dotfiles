source-file ~/.tmux/tmux.conf

setenv -g PROJECT ~/workspace/royaltiz/retool/worktree
setenv -g PATH $PATH:~/workspace/royaltiz/retool/worktree/node_modules/.bin
new-session -s 1_project -n cli -c ~/workspace/royaltiz/retool/worktree
new-window -n vim -c ~/workspace/royaltiz/retool/worktree
new-window -n llm -c ~/workspace/royaltiz/retool/worktree
new-window -n job -c ~/workspace/royaltiz/retool/worktree
new-window -n log -c ~/workspace/royaltiz/retool/worktree
new-window -n app -c ~/workspace/royaltiz/retool/worktree
select-window -t 1_project:cli
