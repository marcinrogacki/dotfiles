# BEHEVIUOR

# set 'n' (nasted) as ssh nasted session prefix
unbind-key C-b
set -g prefix C-n
bind-key C-n send-prefix

# LAYOUT

# some server info
set -g status-right "ssh | ctrl + n | %F %R" # env | prefix | clock
set -g status-bg red
